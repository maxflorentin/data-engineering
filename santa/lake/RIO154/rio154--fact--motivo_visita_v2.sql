-- CITAS.MOTIVO_VISITA_V2 definition

-- DDL generated by DBeaver
-- WARNING: It may differ from actual native database DDL

-- Drop table

-- DROP TABLE CITAS.MOTIVO_VISITA_V2;

CREATE TABLE CITAS.MOTIVO_VISITA_V2 (
	ID_MOTIVO NUMBER(10,0),
	ID_MOTIVO_PADRE NUMBER(10,0),
	DESCRIPCION VARCHAR2(500),
	PRIORIDAD VARCHAR2(1),
	DURACION VARCHAR2(2),
	ID_CANAL NUMBER(10,2),
	ID_SECTOR NUMBER(10,0),
	ORDEN NUMBER DEFAULT 0,
	ACTIVO NUMBER DEFAULT 0,
	TAG VARCHAR2(100),
	ID_TIPO_STACK NUMBER,
	TIENE_FLUJO_SOL NUMBER DEFAULT NULL);