{
    "logTable": "bi_corp_bdr.log_controles",
    "queries": [
        "SELECT grl.partition_date as fecha_proceso, 'R0033344343' as cod_incidencia, count(*) as num_reg_total, sum(case when grl.g5515_grup_eco is null then 1 else 0 end ) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.jm_grup_rela' as tabla from bi_corp_bdr.jm_grup_rela grl left join bi_corp_bdr.jm_grup_econo gec on grl.g5515_grup_eco = gec.G5512_GRUP_ECO and grl.partition_date = gec.partition_date where grl.partition_date = '${month_partition_bdr}' group by grl.partition_date",
        "SELECT grl.partition_date as fecha_proceso, 'R0034344341' as cod_incidencia, count(*) as num_reg_total, sum(case when grl.g5515_idnumcli is null then 1 else 0 end ) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.jm_grup_rela' as tabla  from bi_corp_bdr.jm_grup_rela grl left join bi_corp_bdr.jm_client_bii cbi on grl.g5515_idnumcli = cbi.g4093_idnumcli and grl.partition_date = cbi.partition_date where grl.partition_date = '${month_partition_bdr}' group by grl.partition_date",
        "SELECT grl.partition_date as fecha_proceso, 'R0125344' as cod_incidencia, count(*) as num_reg_total, count(*) - count(distinct g5515_grup_eco, g5515_idnumcli) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.jm_grup_rela' as tabla  from bi_corp_bdr.jm_grup_rela grl where grl.partition_date = '${month_partition_bdr}' group by grl.partition_date",
        "SELECT grl.partition_date as fecha_proceso, 'R1304344' as cod_incidencia, count(*) as num_reg_total, sum(case when bgl.cod_baremo_global is null then 1 else 0 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.jm_grup_rela' as tabla from bi_corp_bdr.jm_grup_rela grl left join (select distinct bgl.cod_baremo_global from bi_corp_bdr.map_baremos_global_local bgl where bgl.cod_negocio = 1 ) bgl on cast(trim(bgl.cod_baremo_global) as int) = cast(trim(grl.g5515_s1emp) as int) where grl.partition_date = '${month_partition_bdr}' group by grl.partition_date",
        "SELECT grl.partition_date as fecha_proceso, 'R1305344' as cod_incidencia, count(*) as num_reg_total, sum(case when bgl.cod_baremo_global is null then 1 else 0 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.jm_grup_rela' as tabla from bi_corp_bdr.jm_grup_rela grl left join (select distinct bgl.cod_baremo_global from bi_corp_bdr.map_baremos_global_local bgl where bgl.cod_negocio = 58 ) bgl on cast(trim(bgl.cod_baremo_global) as int) = cast(trim(grl.g5515_rol_jera) as int) where grl.partition_date = '${month_partition_bdr}' group by grl.partition_date",
        "SELECT partition_date as fecha_proceso, 'J322' as cod_incidencia, count(*) as num_reg_total, sum(case when rlike( G5515_FEOPERAC, '(\\W|^)[0-9]{4}-[0-9]{2}-[0-9]{2}(\\W|$)') then 0 else 1 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.JM_GRUP_RELA' as tabla from bi_corp_bdr.JM_GRUP_RELA where partition_date = '${month_partition_bdr}' group by partition_date",
        "SELECT partition_date as fecha_proceso, 'J323' as cod_incidencia, count(*) as num_reg_total, sum(case when rlike( G5515_S1EMP, '(\\W|^)[0-9]{5}(\\W|$)') then 0 else 1 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.JM_GRUP_RELA' as tabla from bi_corp_bdr.JM_GRUP_RELA where partition_date = '${month_partition_bdr}' group by partition_date",
        "SELECT partition_date as fecha_proceso, 'J324' as cod_incidencia, count(*) as num_reg_total, sum(case when rlike( G5515_GRUP_ECO, '(\\W|^)[0-9]{9}(\\W|$)') then 0 else 1 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.JM_GRUP_RELA' as tabla from bi_corp_bdr.JM_GRUP_RELA where partition_date = '${month_partition_bdr}' group by partition_date",
        "SELECT partition_date as fecha_proceso, 'J325' as cod_incidencia, count(*) as num_reg_total, sum(case when rlike( G5515_IDNUMCLI, '(\\W|^)[0-9]{9}(\\W|$)') then 0 else 1 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.JM_GRUP_RELA' as tabla from bi_corp_bdr.JM_GRUP_RELA where partition_date = '${month_partition_bdr}' group by partition_date",
        "SELECT partition_date as fecha_proceso, 'J326' as cod_incidencia, count(*) as num_reg_total, sum(case when rlike( G5515_ROL_JERA, '(\\W|^)[0-9]{5}(\\W|$)') then 0 else 1 end) as num_reg_error, from_unixtime(unix_timestamp()) as op_timestamp, 'bi_corp_bdr.JM_GRUP_RELA' as tabla from bi_corp_bdr.JM_GRUP_RELA where partition_date = '${month_partition_bdr}' group by partition_date"
    ]
}